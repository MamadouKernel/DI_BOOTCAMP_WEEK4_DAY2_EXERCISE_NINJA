import { AddCustomEventDirective } from './add-custom-event.directive';

describe('AddCustomEventDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCustomEventDirective();
    expect(directive).toBeTruthy();
  });
});
