/* import { ToogleVisibilityDirective } from './toogle-visibility.directive';

describe('ToogleVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
 */