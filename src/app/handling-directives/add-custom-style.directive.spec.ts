import { AddCustomStyleDirective } from './add-custom-style.directive';

describe('AddCustomStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new AddCustomStyleDirective();
    expect(directive).toBeTruthy();
  });
});
